<UserControl x:Class="Ildiamnte_Inventory_System.Views.SetupConnectionMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Ildiamnte_Inventory_System.Views"
             mc:Ignorable="d" Width="Auto" Height="Auto">
    <Border Grid.Column="1" Margin="10" CornerRadius="10" Background="#FFFFCCB9">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Imposta connessione al database" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
            <local:SetupConnectionView Grid.Row="1" DataContext="{Binding}" HorizontalAlignment="Left" VerticalAlignment="Top"/>

        </Grid>
    </Border>
</UserControl>
